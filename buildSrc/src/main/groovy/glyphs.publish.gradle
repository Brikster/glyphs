plugins {
    id 'maven-publish'
}

java {
    withSourcesJar()
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
            artifactId = "$mavenArtifactId"
        }
    }
}